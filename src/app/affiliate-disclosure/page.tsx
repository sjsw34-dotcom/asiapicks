import StaticPage, { staticPageMetadata } from "@/components/content/StaticPage";

export const metadata = staticPageMetadata("affiliate-disclosure");

export default function Page() {
  return <StaticPage slug="affiliate-disclosure" />;
}
